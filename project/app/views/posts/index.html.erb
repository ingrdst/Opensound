
  <div id="sound_show">
	<div id="show_banner">
	    <h1>Updates from Developers</h1>
    </div>
  
  <div id="links">
    <div class="wrapper_skinny">
      <ul>
        <li><%= link_to 'Posts', posts_path(current_sound) %></li>
        <li><%= link_to "Account Settings", edit_sound_registration_path %></li>
				<li><%= link_to "View Sounds",sounds_path %></li>

      </ul>
    </div>
  </div>

	<div id="posts">
    <div class="wrapper_skinny">

<h3>OpenSound Public Releases</h3>
<h4> 1.0.0</h4>
<ul>
<li>Basic Implementations and Funcionalities;</li>
<li>Welcome Page & Front-end;</li>
<li>Authentication Settings;</li>
<li>Dashboard & Private Content;</li>
</ul>

<h4> 1.0.1</h4>
<ul>
<li>User, Tracks & Developer Pages;</li>
<li>Improve App functions and Layout design;</li>
<li>Buttons & Stylesheets </li>
<li>Licenses & About specifications;</li>
</ul>

<% if current_sound %>
<h3>OpenSound Private Releases (Beta!)</h3> 
<h4> 1.0.2</h4>
<ul>
<li>Image multiple-format testing;</li>
<li>MP3 Audio-format;</li>
<li>Search Box & Paginate divisions;</li>
</ul>
<% end %>

<% if current_sound && current_sound.admin %>
<h3>OpenSound Developers Releases (Omega!)</h3> 
<h4> 1.0.3</h4>
<ul>
<li>Audio multiple-format testing;</li>
</ul>
<table>
  <thead>
    <tr>
      <th>Version</th>
      <th>Update</th>
      <th>Developer</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @posts.each do |post| %>
      <tr>
        <td><%= post.title %></td>
        <td><%= post.text %></td>
        <td><%= post.sound.email %></td>
        
        <% if current_sound && current_sound.admin %>
        <td><%= link_to 'Show', post %></td>
        <td><%= link_to 'Edit', edit_post_path(post) %></td>
        <td><%= link_to 'Destroy', post, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
      <% end%>
    <% end %>
  </tbody>
</table>

<br>
<% if current_sound && current_sound.admin %>
<%= link_to 'New Dev Post', new_post_path %><br>
<a href="http://localhost:3000/admin">Devs Settings</a>
<% end %>
<% end %>
<br>
<br>
